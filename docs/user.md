# Примечания

- Документация опирается на версии клиента 1.2.x.
- Документация может содержать синтаксические ошибки и опечатки, в случае их нахождения стоит связаться с автором в Discord/Wire или создать issue на Codeberg.

**Автор документации:** nullclyze (био - https://nullclyze.codeberg.page/)

# Начало

## Для чего предназначен клиент

- **Стресс-тесты:** проверка серверов на мощность.
- **Автоматизация:** автоматическое выполнение простых задач.
- **Тестирование:** проверка анти-бот и анти-чит систем.
- **Фарминг (PvE):** масштабные авто-фермы.

# Раздел "Главная"

Этот раздел является стартовой страницей. Здесь задаётся подключение к серверу и запускаются или останавливаются боты.

## Поля

- `Адрес сервера`: IP или домен с портом, например: `mc.server.com`, `127.0.0.1:25565`. Если не указывать порт, будет установлен 25565 по умолчанию.
- `Кол-во ботов`: Количество запускаемых ботов.
- `Задержка подключений`: Интервал в миллисекундах между подключениями ботов. Рекомендуется ставить значения от `4000` до `7000`. При слишком мелкой задержке твой процессор будет испытывать очень большую нагрузку.

## Кнопки

- **Запустить**: Запуск ботов с текущими настройками. После запуска настройки невозможно изменить (можно менять значение, но на текущем процессе это не отобразится).
- **Остановить**: Отключение всех ботов, очистка данных, выключение служб (график, мониторинг, радар).
- **Генерация**: Случайная подстановка значений в поля (кол-во, задержка).
- **Очистить**: Сброс всех полей на странице.

# Раздел "Настройки"

Данный раздел содержит в себе основную часть настроек и контролирует состояния тех или иных особенностей запуска.

## Основные настройки

**Важно:** При включённой фиче **Использовать аккаунты** эти настройки не будут доступны, ты сможешь изменить их для каждого аккаунта в разделе _Аккаунты_.

### Никнейм

- **Легитный**: Никнейм генерируется на основе реального имени игрока, выбранного при помощи псевдослучайности из списка (всего ~110 никнеймов), и небольшой модификации выбранного имени.
- **Случайный**: Никнейм генерируется на основе простого шаблона, выбранного при помощи псевдослучайности из списка.
- **Кастомный**: Ты сам задаёшь шаблон для псевдослучайной генерации никнейма. Допустимые теги: `#n` (3 случайные цифры от 0 до 9 включительно), `#l` (3 случайные литеры любого регистра), `#m` (3 случайных символа, состоящих из цифр от 0 до 9 включительно и литер любого регистра). Есть ещё тег `#s`, но он включает в себя специальные символы, поэтому для генерации никнеймов он непригоден. Пример шаблонов: `player_#n#n` -> `player_627496`, `#l#n` -> `wKd815`.

### Пароль

- **Без пароля**: Всё просто, боты не будут использовать и хранить пароль.
- **Легитный**: Аналогично никнейму, но список паролей меньше (всего ~10 паролей).
- **Случайный**: Аналогично никнейму.
- **Кастомный**: Аналогично никнейму. Здесь уже можно спокойно использовать тег `#s`, так как пароли со специальными символами разрешены на многих серверах.

### Электронная почта

- **Без почты**: Вновь всё просто, боты не будут использовать и хранить почту.
- **Случайный**: Генерируется псевдослучайная, скорее всего несуществующая почта. Генерация опирается на такие почты как: gmail.com, proton.me, yandex.ru, mail.ru.

## Настройки регистрации

Данный раздел будет доступен после включения фичи **Auto-Register**.

### Режим

- **Обычный**: Боты регистрируются через псевдослучайную задержку, сгенерированную с учётом указанных границ (максимальная и минимальная задержка в миллисекундах). Это подходит тогда, когда у ботов стабильное соединение, известно точное время процессов, которые проходят до момента регистрации.
- **Триггер**: Изначально боты ожидают сообщение в чате, которое содержит указанное вами ключевое слово (допустим, `зарегистрируйтесь`, `register`), после чего мгновенно отправляют сообщение в чат. Подходит при нестабильном подключении, в любых ситуациях, где неизвестно время начала регистрации.

### Команда

Команда регистрации. Например: `/register`, `/reg`.

Если команда не будет указана, процесс регистрации будет прерван.

### Шаблон

Шаблон - это самая важная часть настроек регистрации. Именно он решает как будет выглядеть финальное сообщение.

Допустимые ключевые слова:

- `$cmd`: Заменяется на команду регистрации. Пример: (скажем, выше мы указали команду `/register`) `$cmd qwe123` -> `/register qwe123`.
- `$pass`: Заменяется на пароль, который ты указывал в основных настройках (если включена фича `Использовать аккаунты`, то берётся пароль, заданный для текущего аккаунта). Пример: (допустим, выше мы указали пароль `qwe123`) `$cmd $pass $pass` -> `/register qwe123 qwe123`.
- `$email`: Заменяется на электронную почту, которую ты указывал в основных настройках (вновь исключение, если включена фича **Использовать аккаунты**, то берётся почта, заданная для текущего аккаунта). Пример: `$cmd $pass $email` -> `/register qwe123 jieruy74u@proton.me`.

Если оставить шаблон пустым, будет использован `$cmd $pass`.

## Настройки логина

Данный раздел будет доступен после включения фичи **Auto-Login**.

Всё аналогично настройкам регистрации, исключений нету.

## Настройки переподключения

Данный раздел будет доступен после включения фичи **Auto-Rejoin**.

### Задержка переподключения

В этом поле указывается задержка в миллисекундах, которая будет использована перед повторным подключением. Рекомендуется указывать задержку от `3000` до `8000`.

## Настройки мониторинга

### Частота обновлений

Здесь ты указываешь интервал в миллисекундах между обновлениями данных мониторинга. Рекомендуется ставить значения от `1000` до `5000`.
 
### Длина истории чата

В данном поле указывается максимальная длина видимого чата ботов в мониторинге. При достижении лимита самые старые сообщения будут удалены из истории чата бота. Рекомендуется ставить значения от `25` до `80`. Если мониторинг чата тебя не интересует, можно поставить значение `0`.

## Дополнительные настройки

- `Дистанция прогрузки`: Дальность прорисовки чанков. Рекомендуется ставить значения от `8` до `16`.
- `Ведущая рука`: Ведущая рука у ботов. Доступно два значение: `left` и `right`. Если оставить это поле пустым, для каждого бота будет выбрано псевдослучайное значение.

## Фичи (особенности)

- **Использовать Auto-Register**: При включении боты будут автоматически регистрироваться.
- **Использовать Auto-Login**: При включении боты будут автоматически логиниться.
- **Использовать Duble-Auth**: Требует включенные опции: **Auto-Register**, **Auto-Login**. При включении боты, пройдя процесс регистрации, не будут изменять состояние `login`, тем самым они дальше будут ожидать процесса логина.
- **Использовать Auto-Rejoin**: При включении боты будут автоматически переподключаться к серверу, если их кикнуло.
- **Использовать Chat-Signing**: При включении боты будут автоматически выполнять операцию Chat-Signing.
- **Использовать Auto-Respawn**: При включении боты будут моментально респавнится при событии смерти.
- **Использовать Accept-RP**: При включении боты будут принимать и обрабатывать все пакеты с ресурс паками.
- **Использовать Pathfinder**: При включении боты смогут использовать функции pathfinder'а.
- **Использовать Auto-Script**: При включении и получении события о спавне для каждого бота отдельно будет запускаться написанный тобой ранее сценарий.
- **Использовать прокси**: При включении боты будут использовать указанные тобой прокси (настраивать в разделе _Прокси_).
- **Использовать аккаунты**: При включении на панели управления появится раздел _Аккаунты_, при запуске будут использоваться аккаунты из списка.
- **Использовать анти-капчу**: При включении боты будут использовать анти-капчу (настраивать в разделе _Анти-Капча_).
- **Использовать вебхук**: При включении некоторые данные будут отправляться (настраивать в разделе _Вебхук_).
- **Оптимизация мониторинга**: При включении отправленные профили будут хэшироваться, что позволит избежать бессмысленной отправки одинаковых образцов.
- **Версия сервера**: Версия, под которую будет маскироваться подключение ботов при помощи ViaProxy (требуется установленный Java 17+). По умолчанию `Авто` - клиент подключается своей нативной версией. При использовании прокси вместе с этой опцией прокси будут проигнорированы.

# Раздел "Плагины"

Плагины - это поведения ботов, которые отвечают за автоматическое выполнения определённых операций.

**Важно:** Выключать и включать плагины возможно только **до запуска**.

## Что за "deprecated"?

Эта метка оворит о том, что плагин был обновлён в более новых версиях клиента. Устаревший плагин всё ещё можно использовать, но рекомендуется обновить версию клиента и использовать самые свежие возможности плагина.

# Раздел "Аккаунты"

В этом разделе ты можешь управлять аккаунтами ботов - добавлять, удалять, импортировать...

## Выбор аккаунтов

Чтобы выбрать нужные аккаунты (например, для запуска или просто общего редактирования), нужно нажать на чекбоксы слева от них.

## Генерация аккаунтов

Чтобы сгенерировать аккаунты, тебе нужно указать число генерируемых аккаунтов (от 1 до 255) и шаблон для генерации юзернеймов.

Шаблон может содержать следующие теги:

- `#n`: 3 случайные цифры (от 0 до 9 включительно).
- `#l`: 3 случайные литеры (любого регистра).
- `#m`: Комбинация `#n` и `#l`.
- `#i`: Текущий индекс юзернейма.

Пример шаблона: `bot_#m` -> `bot_m3K`, `bot_i35`, `bot_GtU`...

## Дополнительно

Если тебе нужно задать сразу нескольким аккаунтам одинаковое значение чего-либо, можно выделить нужные аккаунты, нажав на флаг рядом с их юзернеймами, и после нажать на кнопку редактировать все выделенные аккаунты (в правом верхнем углу).

# Раздел "Прокси"

## Список прокси

Прокси вводятся в текстовое поле, по одному на строку. Для SOCKS5 формат: `ip:port`, `socks5://ip:port`, `username:password@ip:port`, `socks5://username:password@ip:port`.

Кнопка **Очистить список** удаляет все записи.

## Сборщик прокси

Сборщик прокси - это специальный инструмент, который позволяет автоматически собирать прокси с публичных ресурсов. Важно понимать, что он не проверяет прокси на работоспособность.

У сборщика есть настройки: алгоритм, страна, количество, порт.

### Алгоритм

Эта опция отвечает за то, какие ресурсы будет использовать сборщик, это прямо влияет на скорость сборки, количество и качество прокси.

Доступно всего 3 алгоритма:

- **Trusted**: Алгоритм для сборка прокси с публичных сервисов, которые предоставляют стабильный API и дополнительную информацию про прокси (именно из-за этого только с этим алгоритмом можно применять фильтр по стране). Поиск с этим алгоритмом обычно занимает ~6 секунд, без ограничений находит ~500 прокси.
- **Untrusted**: Алгоритм, предназначенный для сбора прокси с недоверенных ресурсов (какие-то публичные списки, репозитории с прокси и т.д.). Поиск с этим алгоритмом обычно так же занимает ~6 секунд, без ограничений находит 50,000 прокси (больше нельзя, системное ограничение клиента).
- **Mixed**: Из названия понятно что алгоритм совмещает в себе 2 предыдущих. Поиск с этим алгоритмом может занимать ~8 секунд, без ограничений находит 50,000 (вновь ограничение, ради интереса: если не учитывать системное ограничение, этот алгоритм может находить аж до 430,000 прокси).

### Прочее

Ты можешь включать фильтр по странам и порту, это может облегчить поиск определённых прокси.

# Раздел "Анти-Капча"

В этом разделе ты можешь настроить работу анти-капчи перед запуском. В момент активного процесса сюда будут приходить капчи ботов (если режим решения ручной).

## Что за типы?

- **Web**: Бот будет ловить ссылку из чата по указанному тобой регулярному выражению и обязательной части ссылки, после отправлять ссылку на капчу в раздел _Анти-Капча_, которую можно будет открыть, нажав на кнопку **Решить капчу**.
- **Map (Inventory)**: Бот будет ожидать появления предмета FilledMap (заполненная карта) в инвентаре, после чего создаст изображение капчи и отправит в раздел _Анти-Капча_, решить эту капчу можно там же нажав на кнопку **Решить капчу**.
- **Map (Frame)**: Бот будет ожидать появления сущностей ItemFrame / GlowItemFrame и собирать из них единое изображение до того, как не закончится лимит (колонны * ряды), далее всё аналогично **Map (Inventory)**.

## Настройки Web

- **Регулярное выражение**: Здесь ты указываешь регулярное выражение, при помощи которого будут ловиться ссылки на капчу из чата. По умолчанию используется `https?://[^\s]+`.
- **Обязательная часть ссылки**: Фильтр пойманных ссылок. Если ссылка содержит указанную часть, она проходит, иначе нет. Значения по умолчанию здесь нету.

## Настройки Map

### Общие

- **Режим**: Всё просто, ручное решение капчи или автоматическое.
- **API сервис**: Сервис, который будет решать капчу в автоматическом режиме: `2captcha.com`, `truecaptcha.org` или `Кастом`.
- **API ключ**: Если используется автоматическое решение ты должен указать свой API ключ от выбранного сервиса (для `2captcha.com` подойдёт и ключ RuCaptcha).
- **Имя пользователя**: Указывается только для `truecaptcha.org`.
- **Ссылка на API**: Указывается только для `Кастом`. Это адрес твоего сервера с API, совместимым с 2Captcha (например `http://127.0.0.1/`). Пути `in.php` и `res.php` добавляются автоматически, поэтому указывать их не нужно (если укажешь - они будут отброшены). Если схема не указана, используется `http://`.

### Подтип Inventory

Здесь отсутствуют уникальные настройки.

### Подтип Frame

- **Количество коллон**: Максимальное количество частей капчи, расположенных слева направо.
- **Количество рядов**: Максимальное количество частей капчи, расположенных сверху в низ.
- **Максимальная пауза**: Максимальная пауза между пакетами MapItemData, при истечении её будет считаться, что капча полностью собрана.

# Раздел "Вебхук"

Webhook - это механизм, который позволяет одному приложению мгновенно отправить данные другому при наступлении события.

**Важно:** Данный клиент поддерживает только Discord Webhook (и аналогичные сервисы по похожим структурам запроса).

## Настройки

- **Ссылка**: В этом поле указывается сам URL вебхука.
- **Информация**: Включает или выключает отправку информационных сообщений. 
- **Данные**: Включает или выключает отправку сообщений с данными. 
- **Действия**: Включает или выключает отправку отправку сообщений про действия бота.

# Раздел "Управление"

Это достаточно большой раздел клиента, поэтому мы разделяем его на несколько секторов (более правильно - модулей управления).

## Немного о распределении

Данный клиент позволяет распределять ботов по группам, что является очередной фичей. Задать ботам группы можно в разделе _Мониторинг_ после запуска. Чтобы управлять конкретной группой ботов достаточно указать в разделе _Управление_ в поле **Группа** нужную тебе группу и включать/выключать модули, последующие команды будут принимать только те боты, которые находятся в указанной группе.

Для групп рекомендуется делать понятные названия, желательно просто из литералов (без спец. символов и цифр, например: `afk_bots`, `MobFarm`), это уберёт кучу проблем с некорректным распределением.

## Модуль "Чат"

Начнём с режимов, ведь именно они определяют поведение этого модуля после включения. Всего режимов два:

- **Сообщение**: Одноразовая отправка сообщения в чат от указанных ботов.
- **Спамминг**: Интервальная отправка сообщения в чат от указанных ботов.

Общие опции:

- **Синхронность**: Синхронизирация тайминга отправки сообщений между ботами (одновременная отправка).
- **Глобальный чат**: Автоматическая подстановка `!` в начало любого сообщения. Наверное, тебе покажется эта опция ненужной, ведь можно то же самое сделать самому, но на самом деле здесь не всё так просто: Перед отправкой в чат, сообщения проходят некий конвеер, где на каждом этапе исходный текст предыдущего сообщения мутируется (если указаны определённые опции), соответственно `!` в начале сообщения может быть сдвинут или заменён. Эта же опция решает такую проблему, она подставляет `!` в начало сообщения уже после всех этапов мутации.
- **Мутация текста**: Разрешает мутировать теги в тексте сообщения, например: `number: #n` -> `number: 724`.
- **Магический текст**: Разрешает полностью мутировать все символы текста сообщения. Случайно меняет регистр литералов, изменяет определённые символы на другие похожие (`1` -> `i`, `3` -> `з`). Работает только с русским и английским реестром символов. Не мутирует ссылки (http/https) и упоминания (@username). Пример: `My message` -> `My m3sS4gE`.

Уникальные опции для спамминга:

- **Анти-Повтор**: Позволяет ботам запоминать текст предыдущего сообщения и избегать отправки дубликатов. Естественно это рекомендуется использовать только если в сообщение есть теги для генерации псевдослучайных значений.
- **Обход**: Мутирует текст сообщений, подставляя в начало или конец случайную строку с тегами генерации. Например: `lol` -> `lol | #l#n` -> `lol hRh185`.

Теги для генерации псевдослучайных значений:

- `#n`: 3 случайные цифры (от 0 до 9 включительно).
- `#l`: 3 случайные литеры (любого регистра).
- `#m`: Комбинация `#n` и `#l`.

Экстра-теги:

- `#username`: Юзернейм текущего бота.
- `#password`: Пароль текущего бота.
- `#health`: Здоровье текущего бота.
- `#player`: Случайное имя игрока, находящегося на сервере.
- `#uuid[player]`: UUID указанного игрока.
- `#radar[player]`: Координаты XYZ указанного игрока.

## Модуль "Действия"

Здесь нету никаких замудрённостей, всё просто и понятно, единственное уточнение: Опция `Импульсивность` заставляет ботов выполнять действия с неким импульсом (промежуточными задержками).

## Модуль "Инвентарь"

Доступные режимы:

- **Базовый**: Базовые действия с инвенатрём, простые клики.
- **Взаимодействие**: Режим взаимодействия с предметами. Нужен в тех случаях, если боту нужно зарядить лук, активировать щит, нажать на компас и т.п.
- **Перемещение**: Специальный режим для перемещения предметов (swap-click).

**Важно:** Здесь не работает привычная человеку нумерация. Индексация слотов опирается на официальный протокол Minecraft (есть исключения), оригинальную индексацию можно посмотреть на https://minecraft.wiki/w/Java_Edition_protocol/Inventory. Исключением является команда **Выбрать слот**, при ней индексация идёт от 0 до 8 (то есть только хотбар слоты).

## Модуль "Движение"

Здесь всё вновь просто. Опции работают аналогично опций из сектора _Действия_.

**Важно:** При использовании **pathfinder** и большом количестве запущенных ботов (40 и более) на слабых устройствах могут проявляется "зависания" ботов, если же ресурсов для вычеслений будет недостаточно, ботов просто кикнет.

## Модуль "Анти-АФК"

Существующие режимы:

- **Минимальный**: Самый простой Анти-АФК, боты будут только поворачивать голову.
- **Нормальный**: Позволяет ботам передвигаться и поворачивать голову.
- **Продвинутый**: Комплексный Анти-АФК, позволяет ботам совершать различные действия (махание рукой, шифтинг, джампинг), показывает более релистичное поведение.

## Модуль "Сталкер"

Существующие режимы:

- **Обычный**: Обычное преследование игрока, без дополнительных проверок путей.
- **Продвинутый**: Позволяет ботам использовать pathfinder при приследовании цели, это может быть полезно на неровных территориях.

## Модуль "Полёт"

**Предупреждение:** Данный модуль сделан исключительно в целях исследования и тестирования, использование его на публичных серверах может быть незаконным.

Существующие режимы:

- **Vanilla**: Обычный полёт через изменение velocity по оси Y.
- **Jump-Fly**: Отправка пакетов о движении с подменой координаты Y и тайминговые прыжки.
- **Teleport-Fly**: Простая отправка пакетов о движении с подменой координаты Y.
- **Bug-Fly**: Подмена velocity по оси Y, опираясь на физику взрыва (точнее его отдачи).

## Модуль "Киллаура"

Постоянные настройки:

- **Поведение**: Умеренное поведение имитирует обычного игрока в PvP. Агрессивное заставляет ботов игнорировать повороты головы на цель.
- **Настройки**: Выбор основных настроек киллауры.
- **Цель**: Тип атакуемой цели.

Комплексные настройки:

- **Дистанция атаки**: Радиус действия киллауры в блоках относительно бота.
- **Задержка**: Интервал между повторной атакой.
- **Слот с оружием**: Индекс хотбар слота (0-8).

Фичи:

- **Авто оружие**: Позволяет ботам автоматически брать лучшее оружие по материалу и чарам.
- **Доджинг**: Позволяет ботам уворачиваться после атаки противника. Вместе с фичей **Преследование** боты могут делать комбо удары.
- **Преследование**: Заставляет ботов преследовать цель в указанном диапазоне.
- **Критические удары**: Позволяет ботам прыгать перед ударом, нанося противнику критические удары.

## Модуль "Скаффолд"

**Предупреждение:** Данный модуль сделан исключительно в целях исследования и тестирования, использование его на публичных серверах может быть незаконным.

Существующие режимы:

- **Noob Bridge**: Обычная постройка моста с зажатым шифтом.
- **Ninja Bridge**: Постройка моста с временным зажатием шифта.
- **God Bridge**: Постройка моста без шифта/прыжков.
- **Jump Bridge**: Аналогично God Bridge но с использованием прыжков.

Настройки:

- **Задержка**: Задержка в миллисекундах между проверками блока под ногами.
- **Минимальный взгляд по X**: Ограничение максимальной ротации по оси X при постройке моста.
- **Максимальный взгляд по X**: Ограничение минимальной ротации по оси X при постройке моста.

## Модуль "Анти-Падение"

**Предупреждение:** Данный модуль сделан исключительно в целях исследования и тестирования, использование его на публичных серверах может быть незаконным.

Доступные режимы:

- **Hovering**: Боты будут сбрасывать velocity по оси Y при падении на `0`.
- **Teleport**: Боты будут отправлять пакет о движении с подменой координаты Y (чуть-чуть выше текущей позиции бота).
- **Water-Drop**: Боты будут использовать ведро воды при падении (water-drop).

Настройки:

- **Триггер (растояние до земли)**: Ограничивает использование Анти-Падения через расстояние до земли в блоках.
- **Триггер (скорость падения)**: Ограничивает использование Анти-Падения через velocity по оси Y.
- **Задержка**: Интервал в миллисекундах между проверками территории под ботом.

## Модуль "Лук аим"

Настройки:

- **Цель**: Тип атакуемой цели.
- **Задержка**: Интервал в миллисекундах между выстрелами.
- **Максимальная дистанция**: Ограничение максимальной видимости лук аима в блоках.

Фичи:

- **Предугадывание**: Позволяет ботам предугадывать следующую позицию цели, если та плавно движется в одну сторону.

## Модуль "Стиллер"

Настройки:

- **Цель**: Цель для стиллинга ресурсов.
- **Радиус взаимодействия**: Максимальная дистанция взаимодействия с целью в блоках.
- **Задержка**: Интервал в миллисекундах между повторным стиллингом.

## Модуль "Майнер"

**Важно:** Данный модуль работает только на серверах без анти-чита.

Настройки:

- **Режим**: Поведение ботов при ломании блоков.
- **Направление взгляда по Y**: Максимальная дистанция взаимодействия с целью в блоках.
- **Слот с инструментом**: Индекс хотбар слота (0-8).
- **Задержка**: Интервал в миллисекундах между циклами.

## Модуль "Фермер"

Настройки:

- **Режим**: Определяет скорость действий бота.
- **Задержка**: Интервал в миллисекундах между циклами.

# Рездел "Сценарий"

Здесь ты можешь писать свои сценарии для ботов на Rust, который будет выполняться в оболочке Rhai (документацию и примеры можно найти на https://github.com/rhaiscript/rhai).

**Важно:** Оболочка **Rhai** (то что выполняет твой сценарий) не поддерживает некоторые концепции языка Rust, из-за чего их использование напрямую невозможно. Например: Нельзя использовать `async/.await`, нельзя использовать `_` в качестве названия переменной.

## Доступные функции

Системные функции:

- `sleep(delay: u64)`: Остановка выполнения сценари на время (в секундах).
- `print(str: &str)`: Печать информации в текущий терминал.
- `log(text: &str, class: &str)`: Логгирование информации в журнал клиента.
- `message(name: &str, content: &str)`: Отправка уведомления в клиент.
- `webhook(content: &str)`: Отправка вебхука по ранее указанной ссылке.

Простые функции:

- `chat(mu: &str, message: &str)`: Отправка сообщения в чат от указанного бота(ов).
- `jump(mu: &str)`: Одноразовый прыжок.
- `swing_arm(mu: &str)`: Махнуть рукой.

Основные функции-геттеры:

- `active_bots_count() -> i64`: Получение количества активных ботов.
- `get_bot_id(mu: &str) -> i64`: Получение Entity ID указанного бота.
- `get_bot_ping(mu: &str) -> i64`: Получение пинга указанного бота.
- `get_bot_feet_pos(mu: &str) -> Position`: Получение позиции ног указанного бота.
- `get_bot_eye_pos(mu: &str) -> Position`: Получение позиции глаз указанного бота.
- `get_bot_health(mu: &str) -> f64`: Получение здоровья указанного бота.
- `get_bot_satiety(mu: &str) -> i64`: Получение сытости указанного бота.
- `get_bot_profile(mu: &str) -> Profile`: Получение профиля указанного бота.
- `bot_is_workable(mu: &str) -> bool`: Получение состояния работоспособности указанного бота.

Основные функции-сеттеры:

- `set_jumping(mu: &str, state: bool)`: Установка состояния jumping.
- `set_crouching(mu: &str, state: bool)`: Установка состояния crouching.
- `set_velocity(mu: &str, axis: &str, velocity: f64)`: Установка скорости по определённой оси.
- `set_on_ground(mu: &str, on_ground: bool)`: Установка значения для on_ground.
- `set_old_position(mu: &str, x: f64, y: f64, z: f64)`: Установка значения для old_position.
- `set_no_jump_delay(mu: &str, delay: i64)`: Установка значения для no_jump_delay.
- `set_was_touching_water(mu: &str, state: bool)`: Установка значения для was_touching_water.
- `set_has_impulse(mu: &str, state: bool)`: Установка значения для has_impulse.

Функции, отвечающие за различные действия:

- `start_walking(mu: &str, direction: &str)`: Ходьба по указанному направлению.
- `start_sprinting(mu: &str, direction: &str)`: Спринт по указанному направлению.
- `start_pathfinder(mu: &str, x: i64, z: i64)`: Ходьба на укзанные координаты (pathfinder).
- `stop_move(mu: &str)`: Остановка движения (walk && sprint).
- `stop_pathfinder(mu: &str)`: Остановка задач pathfinder.
- `pathfinder_activity(mu: &str) -> bool`: Получение активности pathfinder.
- `place_block(mu: &str, x: i64, y: i64, z: i64)`: Установка блока на указанных координатах.
- `get_block_id(mu: &str, x: i64, y: i64, z: i64) -> Maybe<&str>`: Получения ID блока на указанных координатах (например, `minecraft:stone`).
- `look_at(mu: &str, x: f64, y: f64, z: f64)`: Поворот на укзанные координаты.
- `look_at_block(mu: &str, x: f64, y: f64, z: f64)`: Поворот на блок с учётом его центра.
- `drop_selected_item(mu: &str, lock: bool)`: Выбросить предмет, лежащий в руке.
- `inv_click(mu: &str, slot: i64, mode: &str, lock: bool)`: Кликнуть определённым образом на указанный слот. Доступные режимы: `left`, `right`, `shift`, `drop`, `drop-all`.
- `inv_click_on(mu: &str, name: &str, mode: &str, lock: bool)`: Кликнуть определённым образом на предмет с указанным именем.
- `set_selected_slot(mu: &str, slot: i64)`: Установка указанного слота в хотбаре.
- `inv_extract_all(mu: &str) -> Array<ItemMeta>`: Извлечение информации о всех предметах в инвентаре указанного бота.
- `inv_extract_by_slot(mu: &str, slot: i64) -> Maybe<ItemMeta>`: Извлечение информации о предмете в указанном слоте.
- `inv_extract_by_name(mu: &str, name: &str) -> Maybe<ItemMeta>`: Извлечение информации о предмете с указанным именем.
- `attack(mu: &str, target: &str, distance: f64, look_at_target: bool)`: Атака определённой ближайшей сущности. Аргумент `target` может иметь значения: any (любая сущность), player, monster, animal, vehicle.
- `start_use_item(mu: &str, hand: &str)`: Начать использование предмета в указанной руке (mainhand / offhand).
- `release_use_item(mu: &str)`: Завершение использования предмета в руке.

Функции, отвечающие за состояния ботов:

- `set_state(mu: &str, field: &str, value: bool)`: Установка значения (true / false) для определённого состояния указанного бота.
- `get_state(mu: &str, field: &str) -> bool`: Получение значения определённого состояния у указанного бота.

Функции, доступные при запуске ботов с фичей "Auto-Script" или раздельного выполнения сценария:

- `current_bot_username() -> &str`: Получение юзернейма текущего бота.
- `current_bot_id() -> i64`: Получение Entity ID текущего бота.
- `current_bot_ping() -> i64`: Получение пинга текущего бота.
- `current_bot_feet_pos() -> Position`: Получение позиции ног текущего бота.
- `current_bot_eye_pos() -> Position`: Получение позиции глаз текущего бота.
- `current_bot_health() -> f64`: Получение здоровья текущего бота.
- `current_bot_satiety() -> i64`: Получение сытости текущего бота.
- `current_bot_profile() -> Profile`: Получение профиля текущего бота.

## Структуры данных

- **Maybe**
  - Либо 0: Данные отсутствуют
  - Либо T: Ожидаемые данные

- **Position**
  - x: `f64`
  - y: `f64`
  - z: `f64`

- **Profile**
  - ping: `i64`
  - captcha_caught: `bool`
  - registered: `bool`
  - logined: `bool`
  - group: `&str`
  - health: `i64`
  - password: `&str`
  - email: `&str`

- **ItemMeta**
  - kind: `&str`
  - count: `i64`
  - current_durability: `i64`
  - max_durability: `i64`
  - enchantments: `Array<(&str, i64)>`
  - slot (optional): `i64`

## Что за состояния ботов?

Состояние бота - это значение в виде флага (true / false) для некого диапазона действий (например: атака, ходьба), влияющее на поведение и возможности бота.

Существует всего 2 вида состояний - `is` и `can`. Is-состояния отвечают за текущее положение бота (к примеру, бот сейчас ходит). Can-состояния отвечают за возможное положение бота (к примеру, сможет ли бот атаковать сущности).

Список всех доступных состояний:

- `can_walking`
- `can_sprinting`
- `can_eating`
- `can_drinking`
- `can_attacking`
- `can_looking`
- `can_interacting`
- `is_walking`
- `is_sprinting`
- `is_eating`
- `is_drinking`
- `is_attacking`
- `is_looking`
- `is_interacting`

## Пояснения

### Аргумент "mu: &str"

Даннный аргумент определяет какой бот попадёт под ей влияние, то есть здесь указывается юзернейм бота. Если указать в этом поле `*`, то функция окажет влияние на всех существующих ботов.

### Аргумент "lock: bool"

Даннный аргумент определяет, можно ли блокировать состояния бота при взаимодействии или же нет.

## Фича "Auto-Script"

Да, это просто обычная фича в разделе _Настройки_, но она имеет особое поведение при включении.

Данная фича автоматически запускает сценарий (который был написан до запуска) для текущего бота, как только тот заспавнится. Если у тебя будут включены фичи **Auto-Register**/**Auto-Login**, то сценарий не запустится сразу, ибо у этих фич более высокий приоретет, сценарий начнёт выполнение только после полного завершения процесса авторизации.

Так же при использовании фичи **Auto-Script** в своём сценарии можно не указывать идентификаторы ботов в аргументах функций, вместо этого просто `""`, ибо явное указание идентификаторов не будет нести какого-то значения в таком случае. Автоматический исполнитель сценария для определённого бота сам захватывает его юзернейм (что является идентификатором) и подставляет его под все нужные функции.

## Примеры сценариев

Отправка 4 сообщений в чат:

```
for i in 0..=3 {
  chat("*", "repeat " + (i + 1)); // Здесь "*" - все боты
  sleep(1.5); // Ожидание 1500 мс
}
```

Простой Anti-AFK:

```
// Да, просто let, в Rhai нету mut переменных
let c = 0;

// Подобные циклы не рекомендуется ставить, ведь
// если в их теле ботам задаются какие-то состояния,
// после ручного выключения они не перестанут быть
// активными. Здесь же с определённым шансом после
// остановки боты всё ещё будут ходить.
while true {
  // Проверка, не выходит ли счётчик
  // за грани массива направлений
  if c > 7 {
    c = 0;
  }

  let directions = [
    "forward",
    "forward-left",
    "forward-right",
    "left",
    "right",
    "backward",
    "backward-right",
    "backward-left"
  ];

  // Запускаем движение по текущему
  // направлению (зависит от счётчика)
  start_walking("*", directions[c]);

  let action_is_enabled = true;

  // В Rhai нету match, поэтому
  // используется конструкция if-else
  if c == 3 {
    set_jumping("*", true);
  } else if c == 6 {
    set_crouching("*", true);
  } else {
    action_is_enabled = false;
  }

  if action_is_enabled {
    sleep(0.5);

    // Остановка действий
    set_jumping("*", false);
    set_crouching("*", false);
  }

  // Создаём паузу на основе счётчика
  let pause = if c % 2 == 0 {
    0.4
  } else {
    1.5
  };

  sleep(pause);

  // Остановка движения всех ботов
  stop_move("*");

  c += 1;
}
```

# Раздел "Мониторинг"

В данном разделе можно мониторить всех ботов, их сетевые состояния, чат, игровые значения.

# Раздел "Радар"

В этом разделе можно отслеживать координаты и маршруты определённых игроков.

**Важно:** Радар имеет ограничение - установленная дальность прорисовки **на сервере**. Если цель (игрок) находится дальше максимальной дистанции прорисовки, боты не смогут получать актуальные данные о ней.

# Раздел "Сканер"

Сканер - это мощная утилита в клиенте, позволяющая за считанные секунды сканировать 1000 хостов на наличие Minecraft серверов.

Настройки:

- **Диапазон (CIDR)**: Диапазон IP-адресов формата `a.b.c.d/n`, например: `185.143.172.0/22`.
- **Количество задач**: Определяет, сколько параллельных задач выделится на сканирование. Рекомендуется ставить значения от `100` до `500`.
- **Целевой порт**: Сканируемый порт, у Minecraft серверов обычно `25565`.
- **Таймаут (мс)**: Максимальное время сканирования одного сервера в миллисекундах. Рекомендуется ставить значения от `4000` до `10000`.

# Раздел "Пинг"

Не понимаешь, доступен целевой сервер или же нет? Нужно получить базовую информацию о сервере? Тогда тебе сюда, вводишь полный адрес сервера и пингуешь его.

# Раздел "Журнал"

Здесь ты можешь смотреть весь журнал событий клиента и дополнительную информацию для отладки.

# Раздел "Конфиг"

Раздел где ты можешь управлять своими конфигурациями - применять, импортировать, архивировать текущие настройки и так далее.

**Важно**: Если при запущенном клиенте ты в ручную добавлял/удалял конфиги из директории `~/.salarixi/configs`, следует нажать на кнопку "обновить" в этом разделе, это синхронизирует конфиги.

# Раздел "Сессия"

## Немного важного

Вообще, в клиенте понятие "сессия" не ограничивается одним разделом. Всё что происходит, запуск ботов, мониторинг, изменение состояний модулей управления и прочее - работа сессии.

## Для чего раздел

В этом разделе ты можешь устанавливать/сбрасывать сессии с удалённым сервером.

Помимо этого у тебя есть возможность обмениваться простыми текстовыми сообщениями с пользователями текущего ядра.

## Для продвинутых пользователей

1. Весь трафик сессии после успешной аутентификации шифруется через AES.
2. Можно запустить ядро (своя обёртка над salarixi-kernel) на любом удалённом сервере.
3. Клиент может взаимодейстовать с ядром, если у них одинаковые версии, или у них отличается только номер PATCH в версии (MAJOR.MINOR.PATCH, например, версии 1.2.0 и 1.2.1 считаются совместимыми). Но на практике это не рекомендуется, ведь даже простые обновления могут быть несовместимы с предыдущим.

# Клиентские настройки

Их можно найти на главной странице клиента (кнопка снизу, ближе к левому краю).

## Глобальные настройки

- `Партиклы`: Показать или скрыть партиклы на фоне.
- `Шрифт`: Глобальный шрифт (кроме панели).
- `Сообщения`: Показать или скрыть всплывающие уведомления.
- `Язык`: Язык интерфейса (русский / английский).
- `Discord RPC`: Включить или выключить отображение активности Discord.

## Настройки панели

- `Иконки`: Показать или скрыть иконки у кнопок на панели.
- `Шрифт`: Шрифт элементов на панели.
- `Размер шрифта`: Размер шрифта элементов на панели.
- `Внутренний отступ`: Внутренний отступ кнопки на панели между иконкой и текстом.
- `Разделители`: Показать или скрыть разделительные линии на левой панели.

## Набор стилей

Наборы стилей - это одна из самых интересных фич клиента, ведь с помощью неё можно от и до кастомизировать стили интерфейса.

### Как это работает?

Всё просто, ты создаёшь JSON файл с инструкциями (где в основном цвета, чуть реже значения размерности и силы), после чего загружаешь его в клиент - Фокус, стили поменялись мгновенно!

### Что за инструкции?

Инструкции (в данном контексте) - это пары рода `"key": "value"` (ключ + значение).

### Создание набора стилей

Создать набор стилей довольно просто:

1. Запусти salarixi (если ты читаешь это не из окна клиента, хах).
2. Создай где-нибудь JSON файл, например `stylekit.json` (главное, чтобы в конце названия было `.json`). 
3. Убедись, что для файла выделены права на запись и чтение (обычно, операционная система Windows или дистрибутивы Linux выделяют нужные права сами при создании файла).
4. Открой созданный файл и напиши в нём `{}`.
5. Далее в этих же скобках можно прописывать инструкции, о них ниже.

Допустимые ключи:

- `title-color`
- `highlight-color`
- `background`
- `primary-bg`
- `primary-border`
- `secondary-bg`
- `secondary-border`
- `bright-bg`
- `bright-border`
- `hover-bg`
- `hover-border`
- `chart-bar-bg`
- `process-inactive-color`
- `process-active-color`
- `process-launching-color`
- `process-stopping-color`
- `cover-bg`
- `cover-border`
- `cover-bg-blur`
- `default-color`
- `muted-color`
- `dim-color`
- `selection-color`
- `info-text-color`
- `input-placeholder`
- `input-border-radius`
- `input-bg`
- `input-border`
- `checkbox-mark-color`
- `checkbox-mark-border-radius`
- `checkbox-bg`
- `checkbox-border`
- `checkbox-hover-bg`
- `checkbox-hover-border`
- `select-mark-color`
- `select-border-radius`
- `select-bg`
- `select-border`
- `select-hover-bg`
- `select-hover-border`
- `dashboard-button-border-radius`
- `dashboard-button-bg`
- `dashboard-button-border`
- `dashboard-button-color`
- `dashboard-button-hover-bg`
- `dashboard-button-hover-border`
- `dashboard-button-hover-color`
- `dashboard-button-selected-bg`
- `dashboard-button-selected-border`
- `dashboard-button-selected-color`
- `button-border-radius`
- `button-bg`
- `button-border`
- `button-color`
- `button-hover-bg`
- `button-hover-border`
- `button-hover-color`
- `button-active-bg`
- `button-active-border`
- `button-active-color`
- `info-log-color`
- `warning-log-color`
- `error-log-color`
- `non-critical-error-log-color`
- `system-log-color`
- `extended-log-color`
- `message-progress-color`
- `message-bg`
- `message-border`
- `message-border-radius`
- `bot-connected-status-color`
- `bot-disconnected-status-color`
- `bot-waiting-status-color`
- `plugin-tag-border-radius`
- `plugin-deprecated-tag-color`
- `plugin-deprecated-tag-bg`
- `plugin-deprecated-tag-border`
- `docs-header-color`
- `docs-code-bg`
- `docs-code-border`
- `docs-navigation-bg`
- `docs-navigation-border`
- `update-notice-warning-color`
- `update-notice-warning-bg`
- `update-notice-warning-border`
- `support-notice-text-bg`
- `support-notice-text-border`
- `scrollbar-bg`
- `scrollbar-thumb-bg`
- `scrollbar-thumb-hover-bg`

По названию ключей можно легко понять какого их назначение:

- `...-color`: Цвет чего-либо (в частности текста). Поддерживает RGB(a), HEX и подобные форматы записи цвета.
- `...-bg`: Сокращение от "background", то есть фон. Поддерживает солидные и градиенты.
- `...-border`: Цвет барьера элемента. Поддержка аналогично `...-color`.
- `...-border-radius`: Радиус барьера. Обычно указывается в пикселях или процентах.

Вот простейший пример набора стилей:

```
{
  "title-color": "#91ff47",
  "highlight-color": "#8af046"
}
```

**Важно:** На конце последней инструкции не должно быть запятой, ибо клиент не сможет извлечь JSON контент из файла.

### Дополнительно

Перед применением твоего набора стилей клиент проверяет количество совпадений (ключи, которые есть и в целевом наборе стилей и в системе клиента), и он отклонит применение только в том случае, если этих совпадений будет 0 (то есть в файле нету инструкций вовсе). Исходя из этого, если тебе нужно поменять цвет одного элемента, совсем не обязательно вписывать все инструкции, клиент автоматически поставит им значение по умолчанию.

Ключи инструкций могут иметь два формата записи:

- `--KEY`: Полный формат записи (да, как переменная в CSS).
- `KEY`: Сокращённый формат записи, тоже корректный.

# Быстрые задачи

Это не является разделом клиента, просто ещё одна фича :3

## Что это такое?

Быстрые задачи - возможность управлять ботами при помощи **горячих клавиш**.

**Важно:** Управлять быстрыми задачами можно только во время **активного процесса**, то есть когда ты запустил ботов на сервер.

## Горячие клавиши

- `Shift + W`: Идти вперёд (продолжительность 200мс).
- `Shift + A`: Идти влево (продолжительность 200мс).
- `Shift + S`: Идти назад (продолжительность 200мс).
- `Shift + D`: Идти вправо (продолжительность 200мс).
- `Shift + I + C`: Полностью очистить инвентарь.
- `Shift + J`: Прыгнуть.
- `Shift + C`: Присесть.
- `Shift + Alt + Q`: Отключиться от сервера.
- `Shift + G + F`: Полёт (изменение velocity по Y на 0.26±5).
- `Shift + G + R`: Прыгнуть и поставить под себя блок.
- `Shift + G + C`: Построить вокруг себя капсулу из блоков.
- `Shift + U`: Объедениться (идти на центральные координаты по X и Z).
- `Shift + T`: Повернуть голову на 50 градусов.
- `Shift + Z`: Повернуть голову в исходное состояние (0.0, 0.0).
- `Shift + F + C`: Сформировать круг (радиус зависит от кол-ва ботов).
- `Shift + F + L`: Сформировать линию.
- `Shift + P + S`: Остановить работу pathfinder.

# Многозадачность и синхронизация

Данный клиент позволяет ботам выполнять множество задач одновременно, что является очередной фичей.

## Синхронизация действий ботов

Синхронизация действий ботов осуществляется при помощи системы состояний. Все плагины и модули управления работают, опираясь на систему состояний, это уменьшает риск декоординации бота и шанс триггеринга анти-чита.

Такая многозадачность является удобной, так как тебе не нужно самому следить за активными модулями и думать "не возникнет ли конфликт если я ещё включу ...?" :3

## Синхронизация в твоём сценарии

Если ты выше читал про допустимые функции в сценарии, то явно обратил внимание на `set_state` и `get_state`. Именно эти функции позволяют напрямую изменять любые состояния указанного бота без каких-либо ограничений. Конечно, это может и не пригодится, ибо почти все функции в сценарии (взаимодействие с инвентарём, ходьба) уже распределяют состояния сами.


